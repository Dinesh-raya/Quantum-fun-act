# Quantum Coins — Version 4 (Qiskit-free)\n\nThis Version 4 removes Qiskit dependencies and uses a small pure-Numpy simulator (qsim) supporting up to 3 qubits.\n\nRun locally:\n1. pip install -r requirements.txt\2. streamlit run app.py\